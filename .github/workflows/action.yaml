name: Test

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build and test
        run: |
          .\Genshin Stella Mod Launcher.exe
